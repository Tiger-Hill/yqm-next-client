import NewPasswordForm from "@/components/auth/users/password/NewPasswordForm";

const EditPasswordPage = ({ params: { lng } }) => {
  return <NewPasswordForm lng={lng} />
};

export default EditPasswordPage;

  // <h2>Reset your Password</h2>
